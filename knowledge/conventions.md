# Conventions

Use the package root `com.example.vehiclemanager`. Organize shared code under `core.data`, `core.domain`, and `core.ui`; organize user-facing functionality under `feature.*`. Keep business logic out of Composables.
