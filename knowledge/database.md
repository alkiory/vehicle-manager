# Database

Room entities and DAOs belong in `core.data`. Foreign-key relationships should enforce data integrity, including cascading child records when a vehicle is deleted. Generated schemas belong in `app/schemas`.
